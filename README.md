# EV CMS Backend New

A minimal Go backend scaffold for EV CMS with Gin, GORM, PostgreSQL, JWT auth, and role-based routing.

## Run

1. Copy .env.example to .env and update values.
2. Start PostgreSQL and create the database.
3. Run:

```bash
go mod download
go run .
```
