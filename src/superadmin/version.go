package superadmin

// Version is replaceable by the deployment build through linker flags. The
// development default is deliberately explicit rather than pretending to be a
// release identifier.
var Version = "development"
